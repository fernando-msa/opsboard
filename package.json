{
  "name": "opsboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "./scripts/start.sh",
    "lint": "next lint",
    "security:audit": "npm audit --omit=dev",
    "security:audit:json": "npm audit --omit=dev --json",
    "security:audit:gate": "node -e \"const {execSync}=require('child_process');let raw='';try{raw=execSync('npm audit --omit=dev --json',{stdio:['ignore','pipe','pipe']}).toString()}catch(e){raw=(e.stdout||'').toString()}const report=JSON.parse(raw||'{}');const v=(report.metadata&&report.metadata.vulnerabilities)||{};const high=Number(v.high||0);const critical=Number(v.critical||0);const moderate=Number(v.moderate||0);const low=Number(v.low||0);console.log('npm audit summary => critical:',critical,'high:',high,'moderate:',moderate,'low:',low);if(high+critical>0){console.error('Security gate failed: high/critical vulnerabilities found.');process.exit(1)}console.log('Security gate passed: no high/critical vulnerabilities.');\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:seed": "tsx prisma/seed.ts",
    "prebuild": "node -e \"const fs=require('fs'); try{fs.unlinkSync('middleware.ts')}catch{}\""
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^2.4.3",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "firebase": "^12.13.0",
    "firebase-admin": "^13.8.0",
    "jose": "^6.2.3",
    "next": "^16.2.4",
    "react": "19.2.6",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^25.9.3",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.2",
    "autoprefixer": "^10.4.20",
    "jsdom": "^29.1.1",
    "postcss": "^8.4.49",
    "prisma": "^7.8.0",
    "tailwindcss": "^3.4.15",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.7"
  }
}
